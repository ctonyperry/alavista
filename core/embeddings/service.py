"""Deprecated misplaced module.

This module accidentally lived at the repository root `core.embeddings`.
Please import the package under `alavista.core.embeddings` instead.
"""

raise ImportError("use alavista.core.embeddings.service instead of core.embeddings")
